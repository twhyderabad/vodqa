section.agenda
  h2 Agenda
  .content
    each track in tracks
        h3= track.title
        each session in track.sessions
          div(class="track "+session.type+" duration-"+session.duration)
            h4.topic= session.topic
            span.speaker= session.speaker
